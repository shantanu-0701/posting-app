<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Profile Page</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white font-sans">

  <nav class="bg-zinc-800 p-4 shadow-lg sticky top-0 z-10">
    <div class="container mx-auto flex justify-between items-center">
      <a href="/" class="text-xl font-bold text-purple-400 hover:text-purple-300">PostingApp</a>
      <div class="flex items-center gap-6">
        <a href="/profile" class="text-white hover:text-purple-300 transition font-medium">My Profile</a>
        <a href="/logout" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-md font-medium">Logout</a>
      </div>
    </div>
  </nav>
  <div class="w-full min-h-screen px-8 py-10">

    <div class="flex justify-center mb-6">
      <div class="w-28 h-28 rounded-full overflow-hidden border-4 border-purple-500 shadow-lg">
        <img src="/images/uploads/<%= user.images %>" alt="Profile Photo" class="w-full h-full object-cover"/>
      </div>
    </div>

    <h3 class="text-center text-4xl font-bold mb-2 tracking-wide">
      Hello, <span class="text-white-400 font-bold"><%= user.name %></span> 👋🏻
    </h3>
    <h5 class="text-center text-lg text-zinc-400 mb-6">You're all set to create your next amazing post!</h5>

    <div class="flex justify-center mb-10">
      <a href="/profile/upload" class="bg-purple-600 hover:bg-purple-700 text-white font-medium py-2 px-5 rounded-md transition">
        Update Profile Picture
      </a>
    </div>

    <div class="flex justify-center">
      <form action="/post" method="post" class="w-full max-w-xl">
        <textarea name="content" rows="4" placeholder="What's on your mind? 🤔"
          class="w-full bg-zinc-800 border border-zinc-700 text-sm p-4 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-500 resize-none mb-4"></textarea>
        <input type="submit" value="Create New Post"
          class="w-full bg-purple-600 hover:bg-purple-700 text-white font-medium py-2 rounded-md transition"/>
      </form>
    </div>

    <div class="mt-16">
      <h3 class="text-xl text-zinc-400 mb-6 font-medium text-center">Your Posts</h3>
      <div class="space-y-8">
      <% user.posts.forEach((post, index) => { %>
        <div class="post w-full max-w-md mx-auto bg-gradient-to-br from-zinc-800 to-zinc-900 p-6 rounded-2xl border border-zinc-700 shadow-xl transition-transform hover:scale-[1.02] duration-300">
          
          <h4 class="text-yellow-400 font-semibold text-xl mb-3 tracking-wide">
            <%= user.username %>
          </h4>
          
          <p class="text-base text-zinc-200 leading-relaxed mb-4">
            <%= post.content %>
          </p>
          
          <small class="text-sm text-zinc-400 block mb-4">
            ❤️ <%= post.likes.length %> Likes
          </small>

          <div class="btns flex flex-wrap gap-4 text-sm text-zinc-400">
            <form action="/like/<%= post._id %>" method="POST">
              <button type="submit" class="hover:text-pink-400 transition duration-200">
                <%= post.likes.indexOf(user._id) === -1 ? "❤️ Like" : "👎 Unlike" %>
              </button>
            </form>
            <a href="/edit/<%= post._id %>" class="hover:text-blue-400 transition duration-200">
              ✏️ Edit Post
            </a>
          </div>
        </div>
      <% }); %>
      </div>
    </div>
  </div>
</body>
</html>