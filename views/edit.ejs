<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Page</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white font-sans">
  <div class="w-full min-h-screen px-8 py-10">

    <!-- Logout Button -->
    <div class="flex justify-end mb-4">
      <a href="/logout" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-md shadow font-medium">
        Logout
      </a>
    </div>
    <div>
  <h2 class="text-center text-2xl font-semibold text-zinc-600 mb-8">
    Want to edit your post?
  </h2>
</div>

    <!-- Create Post Form -->
    <div class="flex justify-center">
      <form action="/update/<%= post._id%>" method="post" class="w-full max-w-xl">
        <textarea name="content" rows="4" placeholder="Something New? 🤔"
          class="w-full bg-zinc-800 border border-zinc-700 text-sm p-4 rounded-lg focus:outline-none focus:ring-2 focus:ring-yellow-500 resize-none mb-4"></textarea>
        <input type="submit" value="Update Post"
          class="w-full bg-yellow-600 hover:bg-yellow-700 text-white font-medium py-2 rounded-md transition"/>
      </form>
    </div>
  </div>
</body>
</html>